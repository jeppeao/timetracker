import styles from "./blockView.module.css"

interface BlockViewProps {

}

const BlockView = (props: BlockViewProps) => {
  return (
    <div>
     Blockview
    </div>
  )
}

export default BlockView;